// Copy this file to config.js and fill in the values.
// config.js is gitignored — values are injected by CI from repository secrets.
// The Supabase anon key is public-safe by design (security comes from RLS + SECURITY DEFINER).
const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
const APP_STORE_URL = 'https://apps.apple.com/app/YOUR_APP_ID';
